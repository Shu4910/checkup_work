# 社員活動モニタリングアプリケーション

Windows環境で社員のPC活動を記録・モニタリングするアプリケーションです。

## 概要

このアプリケーションは、社員が業務時間中にどのようなアプリケーション・Webサイトを利用しているかを記録し、生産性の可視化・改善に役立てることを目的としています。

## 主な機能

- **アクティビティ記録**: 60秒ごとにアクティブなアプリケーションとウィンドウタイトルを記録
- **ブラウザURL取得**: Chrome、Edge、Firefoxの現在表示中のURLとページタイトルを取得
- **スリープ検知**: PCスリープ時は自動的にポーリングを停止（バッテリー節約）
- **ロック検知**: PCロック時は状態を`locked`として記録
- **CSVログ出力**: 1日1ファイルでCSV形式で保存
- **システムトレイ常駐**: システムトレイにアイコンを表示し、常駐実行
- **自動ログローテーション**: 90日以上古いログファイルは自動削除

## システム要件

- **OS**: Windows 10 (64bit) / Windows 11
- **Python**: 3.12以降
- **メモリ**: 50MB以上の空きメモリ
- **ディスク**: ログ保存用の空き容量

## インストール

### 1. 依存パッケージのインストール

```bash
pip install -r requirements.txt
```

### 2. pywin32のインストール後処理

pywin32をインストールした後、以下のコマンドを実行してください。

```bash
python -m pywin32_postinstall -install
```

## 設定

`config.json`で以下の設定が可能です。

```json
{
    "log_directory": "C:\\ProgramData\\ActivityMonitor\\logs",
    "polling_interval": 60,
    "log_retention_days": 90,
    "admin_password": "admin123"
}
```

| 設定項目 | 説明 | デフォルト値 |
|---------|------|-------------|
| `log_directory` | ログ保存先ディレクトリ | `C:\ProgramData\ActivityMonitor\logs` |
| `polling_interval` | ポーリング間隔（秒） | 60 |
| `log_retention_days` | ログ保存期間（日数） | 90 |

## 使い方

### 起動

```bash
python src/activity_monitor.py
```

アプリケーションが起動すると、システムトレイにアイコンが表示されます。

### システムトレイメニュー

- **現在の状態を表示**: 現在のモニタリング状態を確認
- **終了**: アプリケーションを終了（確認ダイアログが表示されます）

### ログファイル

ログは以下の形式で保存されます。

- ファイル名: `activity_log_YYYY-MM-DD.csv`
- 保存先: `config.json`で指定したディレクトリ

#### CSVフォーマット

```csv
timestamp,machine_name,user_name,process_name,window_title,url,page_title,status
2026-02-22 09:01:00,PC-001,tanaka,chrome.exe,Google Chrome,https://mail.google.com/,受信トレイ - Gmail,active
2026-02-22 09:02:00,PC-001,tanaka,excel.exe,売上管理表.xlsx - Microsoft Excel,,,active
2026-02-22 09:03:00,PC-001,tanaka,,,,,locked
```

## 自動起動設定（オプション）

### Windowsタスクスケジューラを使用

1. タスクスケジューラを開く
2. 「基本タスクの作成」を選択
3. トリガーを「コンピューターの起動時」に設定
4. 操作を「プログラムの起動」に設定
5. プログラム: `pythonw.exe`
6. 引数: `src\activity_monitor.py`の絶対パス
7. 開始: プロジェクトのルートディレクトリ

## トラブルシューティング

### ブラウザURLが取得できない

- UIAutomationがブラウザのアドレスバーにアクセスできない場合があります
- ブラウザの言語設定が日本語でない場合、アドレスバーの名前が一致しない可能性があります
- その場合、`browser_tracker.py`の`Name`パラメータを調整してください

### システムトレイアイコンが表示されない

- Pillowが正しくインストールされているか確認してください
- Windowsの通知領域設定で、アイコンが非表示になっていないか確認してください

## 法的・倫理的考慮事項

- 就業規則・雇用契約への明記、または社員への事前告知が推奨されます
- システムトレイアイコンにより、社員はモニタリング中であることを認識できます
- 個人情報保護法の観点から、収集データの管理・アクセス制限を適切に行ってください
- 収集したデータを目的外利用しないでください
- 本アプリケーションの利用は、日本の労働関連法規に準拠して行ってください

## ライセンス

本アプリケーションは、仕様書に基づいて作成されたものです。

## バージョン

- **バージョン**: 1.0.0
- **作成日**: 2026-02-22
